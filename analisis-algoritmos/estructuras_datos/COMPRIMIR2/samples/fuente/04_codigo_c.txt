#include <stdio.h>

void print_sequence(int n) {
    for (int i = 0; i < n; i++) {
        printf("f(%d) = %d\n", i, i * i);
    }
}

int main(void) {
    for (int i = 0; i < 10; i++) {
        print_sequence(5);
        printf("\n");
    }
    return 0;
}
